
<%# PARA HACER EL GRID, SE HACE POR ARRIBA DE LA ITERACIÓN %>
<% if params[:query] %>
  <% @gifts.each do |gift| %>
    <% if gift.searchable.respond_to?(:user_name) %>
      <div class="card">
        <%= gift.searchable.user_name %>
        <%# <%= render "search_user", gift: gift %>
      </div>
    <% else %>
      <%= render "search_gift", gift: gift %>
    <% end %>
  <% end %>
<% else %>
  <% @gifts.each do |gift| %>
    <%= render "lists/gift_card", gift: gift %>
  <% end %>
<% end %>
