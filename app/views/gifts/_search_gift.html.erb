<div class="col-12">
  <div class="card-gift">
    <div class="">
    <%= '<span class="new-gift-badge badge bg-success small">new</span>'.html_safe if element.searchable.created_at >= (Date.today -7)  %>
      <div class="gift-img-container" >
        <% if element.searchable.photo.key == nil%>
        <% else %>
          <%= cl_image_tag element.searchable.photo.key %>
        <% end %>
      </div>
      <p class=" gift-price">$ <%= number_with_precision(element.searchable.price, :precision => 0, :delimiter => ',') %></p>
      <h2 class="">
        <%= link_to "#{element.searchable.title}", gift_path(element), data: { turbo_frame: "modal" }%>
      </h2>
      <p class="gift-info text-capitalize">
      <% if element.searchable.description?%>
        <% if element.searchable.description.length > 40 %>
          <%= truncate(element.searchable.description, length: 40) %>
        <%= link_to '...Leer más', gift_path(element.searchable.id), data: { turbo_frame: "modal" } %>
        <% else %>
          <%= element.searchable.description %>
        <% end %> </p>
      <% else %>
        "Sin detalle"
      <% end %> </p>

      <div class="gift-bottom">
        <%= link_to "Ver Mas", gift_path(element.searchable.id), class: "btn btn-primary", data: { turbo_frame: "modal" } %>
      </div>
    </div>
  </div>
</div>
